spring:
  cloud:
    config:
      server:
        git:
          uri: git@github.com:ocristian/microservices-pla.git
          search-paths:
            - "pla-config-properties/{application}/{profile}"